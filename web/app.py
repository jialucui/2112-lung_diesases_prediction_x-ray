from fastapi import FastAPI\nfrom pydantic import BaseModel\n\napp = FastAPI()\n\nclass PneumoniaRequest(BaseModel):\n    data: list    # Input data for prediction\n\n@app.post('/api/v1/predict')\ndef predict_pneumonia(request: PneumoniaRequest):\n    # Placeholder for prediction logic\n    return {'prediction': 'pneumonia or no pneumonia'}\n\n@app.get('/api/v1/health')\ndef health_check():\n    return {'status': 'healthy'}\n\n@app.get('/api/v1/info')\ndef info():\n    return {'app': 'Pneumonia Detection API', 'version': '1.0'}